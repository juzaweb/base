/** Shared email regex for form validation */
export const EMAIL_REGEX = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i;

/** Minimum password length (must match backend policy) */
export const MIN_PASSWORD_LENGTH = 8;
